#include "pch.h"
#include "DataManager.h"

map<int, Protocol::CharacterMetaData> DataManager::CharacterMap;
map<int, Protocol::SkillMetaData> DataManager::SkillMap;
map<int, Protocol::ConsumMetaData> DataManager::ConsumMap;
map<int, Protocol::EquipMetaData> DataManager::EquipMap;
map<int, Protocol::NPCMetaData> DataManager::NPCMap;
map<int, Protocol::ProjectileMetaData> DataManager::ProjectileMap;
map<int, Protocol::SummonsMetaData> DataManager::SummonsMap;
map<int, Protocol::EnemyMetaData> DataManager::EnemyMap;
